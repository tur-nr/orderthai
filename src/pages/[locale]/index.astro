---
import type { GetStaticPaths } from "astro";
import { locales } from "../../i18n/config";
import Eat from "./eat/_eat.astro";

export const getStaticPaths = (() =>
  locales.map((locale) => ({
    params: {
      locale,
    },
  }))) satisfies GetStaticPaths;
---

<Eat />
